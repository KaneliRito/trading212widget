<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Proton Light Theme - Inspired by Proton Mail/Drive -->
    
    <!-- Colors -->
    <Color x:Key="ProtonPurple">#6D4AFF</Color>
    <Color x:Key="ProtonPurpleHover">#7C5CFF</Color>
    <Color x:Key="ProtonPurplePressed">#5D3AEF</Color>
    
    <Color x:Key="BackgroundPrimary">#FFFFFF</Color>
    <Color x:Key="BackgroundSecondary">#F5F5F7</Color>
    <Color x:Key="BackgroundTertiary">#EBEBED</Color>
    
    <Color x:Key="TextPrimary">#1C1B22</Color>
    <Color x:Key="TextSecondary">#5E5E62</Color>
    <Color x:Key="TextTertiary">#8C8C91</Color>
    
    <Color x:Key="BorderColor">#DADADE</Color>
    <Color x:Key="BorderHover">#C4C4C8</Color>
    
    <Color x:Key="SuccessColor">#2E7D32</Color>
    <Color x:Key="ErrorColor">#C62828</Color>
    <Color x:Key="WarningColor">#FF9800</Color>
    
    <!-- Solid Color Brushes -->
    <SolidColorBrush x:Key="ProtonPurpleBrush" Color="{StaticResource ProtonPurple}"/>
    <SolidColorBrush x:Key="ProtonPurpleHoverBrush" Color="{StaticResource ProtonPurpleHover}"/>
    <SolidColorBrush x:Key="ProtonPurplePressedBrush" Color="{StaticResource ProtonPurplePressed}"/>
    
    <SolidColorBrush x:Key="BackgroundPrimaryBrush" Color="{StaticResource BackgroundPrimary}"/>
    <SolidColorBrush x:Key="BackgroundSecondaryBrush" Color="{StaticResource BackgroundSecondary}"/>
    <SolidColorBrush x:Key="BackgroundTertiaryBrush" Color="{StaticResource BackgroundTertiary}"/>
    
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
    <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiary}"/>
    
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="BorderHoverBrush" Color="{StaticResource BorderHover}"/>
    
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>

    <!-- Typography -->
    <FontFamily x:Key="ProtonFont">Segoe UI, Arial, sans-serif</FontFamily>
    
    <!-- Styles (same as Dark theme) -->
    <Style x:Key="ProtonButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource ProtonPurpleBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="{StaticResource ProtonFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ProtonPurpleHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ProtonPurplePressedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ProtonIconButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource BackgroundTertiaryBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ProtonTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource ProtonFont}"/>
    </Style>

    <Style x:Key="ProtonTitle" TargetType="TextBlock" BasedOn="{StaticResource ProtonTextBlock}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="ProtonValue" TargetType="TextBlock" BasedOn="{StaticResource ProtonTextBlock}">
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style x:Key="ProtonLabel" TargetType="TextBlock" BasedOn="{StaticResource ProtonTextBlock}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>

    <Style x:Key="ProtonSubValue" TargetType="TextBlock" BasedOn="{StaticResource ProtonTextBlock}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <Style x:Key="ProtonTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource BackgroundTertiaryBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="FontFamily" Value="{StaticResource ProtonFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Shadow Effect -->
    <DropShadowEffect x:Key="ProtonShadow" 
                      BlurRadius="20" 
                      ShadowDepth="0" 
                      Opacity="0.15" 
                      Color="#000000"/>

</ResourceDictionary>
